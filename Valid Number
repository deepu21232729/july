class Solution:
    def isNumber(self, s: str) -> bool:
        try:
            if 'inf' in s or 'Inf' in s:
                return False             
            else:
                float(s)                
        except:
            return False
        else:
            return True 
